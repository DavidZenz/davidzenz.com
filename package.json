{
  "name": "davidzenz-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site for davidzenz.com",
  "scripts": {
    "fetch:github": "node scripts/fetch-github.mjs",
    "fetch:gigs": "node scripts/fetch-gigs.mjs",
    "build:images": "node scripts/build-images.mjs",
    "build": "npm run fetch:github && npm run fetch:gigs && npm run build:images && eleventy",
    "start": "npm run fetch:github && npm run fetch:gigs && npm run build:images && eleventy --serve",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.35.3"
  }
}
